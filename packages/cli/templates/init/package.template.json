{
	"name": "{{PROJECT_NAME}}",
	"private": true,
	"type": "module",
	"scripts": {
		"collect": "harbor collect",
		"post-collect": "harbor post-collect",
		"build": "harbor build",
		"deploy:cloudflare": "harbor deploy cloudflare-pages",
		"dev": "harbor dev",
		"preview": "harbor preview"
	},
	"dependencies": {
		"agent-skill-harbor": "{{PACKAGE_RANGE}}",
		"agent-skill-harbor-collector": "{{COLLECTOR_RANGE}}",
		"agent-skill-harbor-post-collect": "{{POST_COLLECT_RANGE}}",
		"agent-skill-harbor-web": "{{WEB_RANGE}}"
	}
}
